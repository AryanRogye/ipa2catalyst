import SwiftUI

struct ContentView: View {
    var body: some View {
        DropZoneView()
            .frame(width: 968, height: 528)
            .background(Color(red: 0.05, green: 0.05, blue: 0.05))
    }
}

#Preview {
    ContentView()
}
